import React from "react"
import './Layout.scss'

const Layout = () => {

    return(
        <div>
            <header>
                <h1>Calculator</h1>
                <h1>The work is done for the portfolio</h1>
            </header>
        </div>
    )
}

export {Layout}